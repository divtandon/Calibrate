version: 2

models:
  - name: stg_orders_cleansed
    description: One row per TPC-H order, cast and null-guarded.
    columns:
      - name: order_key
        tests: [unique, not_null]
      - name: total_price
        tests: [not_null]
